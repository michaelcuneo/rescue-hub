{
  "name": "rescue-hub",
  "version": "1.0.0",
  "private": true,
  "description": "Rescue Hub — NSW wildlife rescue coordination platform",
  "license": "ISC",
  "engines": {
    "node": ">=22.19.0"
  },
  "packageManager": "npm@10.9.8",
  "scripts": {
    "dev": "sst dev",
    "sst:install": "sst install",
    "deploy": "sst deploy",
    "deploy:production": "sst deploy --stage production",
    "remove": "sst remove",
    "check": "npm run check --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "format": "npm run format --workspaces --if-present",
    "dev:web": "npm run dev --workspace=website",
    "dev:mobile": "npm run start --workspace=@rescue-hub/mobile",
    "mobile:ios": "npm run ios --workspace=@rescue-hub/mobile",
    "mobile:android": "npm run android --workspace=@rescue-hub/mobile"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "sst": "^4.17.1",
    "typescript": "^6.0.3"
  }
}
